# Groq API Configuration (Required)
GROQ_API_KEY=your_groq_api_key_here

# Flask Configuration
FLASK_ENV=development
FLASK_PORT=5000
DATABASE_PATH=./chatbot.db
FRONTEND_URL=http://localhost:5173

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here_change_in_production
JWT_EXPIRES_IN=7d

# Gmail API Configuration (Required for OTP emails)
# Note: We use Gmail API (not SMTP) for reliable email delivery
# Run 'python gmail_auth.py' to generate token, then paste the entire token.json content below
GMAIL_TOKEN={"token":"ya29...","refresh_token":"1//...","token_uri":"https://oauth2.googleapis.com/token","client_id":"...","client_secret":"...","scopes":["https://www.googleapis.com/auth/gmail.send"]}
EMAIL_SENDER=your-email@gmail.com
